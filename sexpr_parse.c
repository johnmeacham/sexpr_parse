/* Generated by re2c 1.3 on Tue Apr 20 22:10:25 2021 */
#line 1 "sexpr_parse.c.re"

#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <bsd/string.h>
#include <stdbool.h>
#include "sexpr_parse.h"


static void
push(struct parse_state *ps,  uintptr_t c)
{
        uintptr_t *ts = ps->stack + ps->sptr++;
        struct centry *ctop =  ps->control_stack + ps->csptr - 1;
        *ts = c;
        while (ctop->unary && ps->sptr - 1 == ctop->depth) {
                *ts = sp_unary(ps, ctop->what, *ts);
                ctop--; ps->csptr--;
        }
}


static void
push_control(struct parse_state *ps, char control)
{
        struct centry *ctop = ps->control_stack + ps->csptr++;
        ctop->what = control;
        ctop->unary = control != '(' && control != '.';
        ctop->depth = ps->sptr;
}

static int
close_enclosed(struct parse_state *ps, char control)
{
        struct centry *ctop = ps->control_stack + ps->csptr - 1;
        bool is_cons = false;
        uintptr_t cdr = 0;
        int sptr = ps->sptr;
        if (ctop->what == '.') {
                ps->csptr--;
                int len = sptr - ctop->depth;
                if (len != 1) {
                        if (ps->fname)
                                printf("%s:%i: ", ps->fname, ps->lineno);
                        printf("bad cons tail %i\n", len);
                        return 6;
                }
                cdr = ps->stack[--sptr];
                ctop--;
                is_cons = true;
        }
        if (ctop->what == '(') {
                ps->csptr--;
                int len = sptr - ctop->depth;
                uintptr_t v = is_cons
                              ?  sp_cons(ps, ctop->what, &ps->stack[sptr - len], len, cdr)
                              :  sp_list(ps, ctop->what, &ps->stack[sptr - len], len);
                ps->sptr = sptr - len;
                push(ps, v);
                return 0;
        }
        if (ps->fname)
                printf("%s:%i: ", ps->fname, ps->lineno);
        printf("bad closing %c %02x\n", ctop->what, ctop->what);
        return 5;
}


int
scan(struct parse_state *ps, char *start)
{
        ps->cursor = ps->start = start;
        char *t;
        int res = 0;
        char *YYMARKER;
        while (!res) {
                t = ps->cursor;
                
#line 82 "sexpr_parse.c"
		{
			unsigned char curr;
			unsigned int yyaccept = 0;
			curr = (unsigned char)*ps->cursor;
			switch (curr) {
			case 0x00:	goto scan2;
			case '\t':
			case '\r':
			case ' ':	goto scan6;
			case '\n':	goto scan9;
			case '!':
			case '$':
			case '%':
			case '&':
			case '*':
			case '/':
			case ':':
			case '<':
			case '=':
			case '>':
			case '?':
			case 'A':
			case 'B':
			case 'C':
			case 'D':
			case 'E':
			case 'F':
			case 'G':
			case 'H':
			case 'I':
			case 'J':
			case 'K':
			case 'L':
			case 'M':
			case 'N':
			case 'O':
			case 'P':
			case 'Q':
			case 'R':
			case 'S':
			case 'T':
			case 'U':
			case 'V':
			case 'W':
			case 'X':
			case 'Y':
			case 'Z':
			case '^':
			case '_':
			case 'a':
			case 'b':
			case 'c':
			case 'd':
			case 'e':
			case 'f':
			case 'g':
			case 'h':
			case 'i':
			case 'j':
			case 'k':
			case 'l':
			case 'm':
			case 'n':
			case 'o':
			case 'p':
			case 'q':
			case 'r':
			case 's':
			case 't':
			case 'u':
			case 'v':
			case 'w':
			case 'x':
			case 'y':
			case 'z':
			case '~':	goto scan11;
			case '"':	goto scan14;
			case '#':	goto scan15;
			case '\'':
			case '(':
			case '`':	goto scan17;
			case ')':	goto scan18;
			case '+':	goto scan20;
			case ',':	goto scan22;
			case '-':	goto scan23;
			case '.':	goto scan25;
			case '0':	goto scan26;
			case '1':
			case '2':
			case '3':
			case '4':
			case '5':
			case '6':
			case '7':
			case '8':
			case '9':	goto scan28;
			case ';':	goto scan30;
			default:	goto scan4;
			}
scan2:
			++ps->cursor;
#line 95 "sexpr_parse.c.re"
			{ res = ps->sptr == 1 ? 0 : 2;  break; }
#line 186 "sexpr_parse.c"
scan4:
			++ps->cursor;
scan5:
#line 111 "sexpr_parse.c.re"
			{ if (ps->fname) printf("%s:%i: ", ps->fname, ps->lineno); printf("unknown character %c\n", *t); res = 1;  continue; }
#line 192 "sexpr_parse.c"
scan6:
			curr = (unsigned char)*++ps->cursor;
			switch (curr) {
			case '\t':
			case '\r':
			case ' ':	goto scan6;
			default:	goto scan8;
			}
scan8:
#line 98 "sexpr_parse.c.re"
			{ continue; }
#line 204 "sexpr_parse.c"
scan9:
			++ps->cursor;
#line 97 "sexpr_parse.c.re"
			{ ps->lineno++; continue; }
#line 209 "sexpr_parse.c"
scan11:
			curr = (unsigned char)*++ps->cursor;
			switch (curr) {
			case '!':
			case '$':
			case '%':
			case '&':
			case '*':
			case '+':
			case '-':
			case '.':
			case '/':
			case '0':
			case '1':
			case '2':
			case '3':
			case '4':
			case '5':
			case '6':
			case '7':
			case '8':
			case '9':
			case ':':
			case '<':
			case '=':
			case '>':
			case '?':
			case '@':
			case 'A':
			case 'B':
			case 'C':
			case 'D':
			case 'E':
			case 'F':
			case 'G':
			case 'H':
			case 'I':
			case 'J':
			case 'K':
			case 'L':
			case 'M':
			case 'N':
			case 'O':
			case 'P':
			case 'Q':
			case 'R':
			case 'S':
			case 'T':
			case 'U':
			case 'V':
			case 'W':
			case 'X':
			case 'Y':
			case 'Z':
			case '^':
			case '_':
			case 'a':
			case 'b':
			case 'c':
			case 'd':
			case 'e':
			case 'f':
			case 'g':
			case 'h':
			case 'i':
			case 'j':
			case 'k':
			case 'l':
			case 'm':
			case 'n':
			case 'o':
			case 'p':
			case 'q':
			case 'r':
			case 's':
			case 't':
			case 'u':
			case 'v':
			case 'w':
			case 'x':
			case 'y':
			case 'z':
			case '~':	goto scan11;
			default:	goto scan13;
			}
scan13:
#line 99 "sexpr_parse.c.re"
			{ push(ps, sp_symbol(ps, t, ps->cursor)); continue; }
#line 298 "sexpr_parse.c"
scan14:
			yyaccept = 0;
			curr = (unsigned char)*(YYMARKER = ++ps->cursor);
			if (curr <= 0x00) goto scan5;
			goto scan34;
scan15:
			curr = (unsigned char)*++ps->cursor;
			switch (curr) {
			case ';':	goto scan40;
			default:	goto scan16;
			}
scan16:
#line 109 "sexpr_parse.c.re"
			{ push_control(ps, *t); continue; }
#line 313 "sexpr_parse.c"
scan17:
			++ps->cursor;
			goto scan16;
scan18:
			++ps->cursor;
#line 110 "sexpr_parse.c.re"
			{ res = close_enclosed(ps, *t); continue; }
#line 321 "sexpr_parse.c"
scan20:
			++ps->cursor;
#line 105 "sexpr_parse.c.re"
			{ push(ps, sp_symbol(ps, t, ps->cursor)); continue; }
#line 326 "sexpr_parse.c"
scan22:
			curr = (unsigned char)*++ps->cursor;
			switch (curr) {
			case '@':	goto scan42;
			default:	goto scan16;
			}
scan23:
			curr = (unsigned char)*++ps->cursor;
			switch (curr) {
			case '0':	goto scan26;
			case '1':
			case '2':
			case '3':
			case '4':
			case '5':
			case '6':
			case '7':
			case '8':
			case '9':	goto scan28;
			default:	goto scan24;
			}
scan24:
#line 106 "sexpr_parse.c.re"
			{ push(ps, sp_symbol(ps, t, ps->cursor)); continue; }
#line 351 "sexpr_parse.c"
scan25:
			yyaccept = 1;
			curr = (unsigned char)*(YYMARKER = ++ps->cursor);
			switch (curr) {
			case '.':	goto scan44;
			default:	goto scan16;
			}
scan26:
			curr = (unsigned char)*++ps->cursor;
			switch (curr) {
			case '0':
			case '1':
			case '2':
			case '3':
			case '4':
			case '5':
			case '6':
			case '7':
			case '8':
			case '9':	goto scan45;
			default:	goto scan27;
			}
scan27:
#line 102 "sexpr_parse.c.re"
			{ push(ps, sp_number(ps, t, ps->cursor, 10)); continue; }
#line 377 "sexpr_parse.c"
scan28:
			curr = (unsigned char)*++ps->cursor;
			switch (curr) {
			case '0':
			case '1':
			case '2':
			case '3':
			case '4':
			case '5':
			case '6':
			case '7':
			case '8':
			case '9':	goto scan28;
			default:	goto scan27;
			}
scan30:
			curr = (unsigned char)*++ps->cursor;
			switch (curr) {
			case 0x00:
			case '\n':	goto scan32;
			default:	goto scan30;
			}
scan32:
#line 96 "sexpr_parse.c.re"
			{ continue; }
#line 403 "sexpr_parse.c"
scan33:
			curr = (unsigned char)*++ps->cursor;
scan34:
			switch (curr) {
			case 0x00:	goto scan35;
			case '"':	goto scan36;
			case '\\':	goto scan38;
			default:	goto scan33;
			}
scan35:
			ps->cursor = YYMARKER;
			switch (yyaccept) {
			case 0: 	goto scan5;
			case 1: 	goto scan16;
			default:	goto scan37;
			}
scan36:
			++ps->cursor;
scan37:
#line 100 "sexpr_parse.c.re"
			{ push(ps, sp_string(ps, t, ps->cursor)); continue; }
#line 425 "sexpr_parse.c"
scan38:
			curr = (unsigned char)*++ps->cursor;
			switch (curr) {
			case 0x00:	goto scan35;
			case '"':	goto scan48;
			case '\\':	goto scan38;
			default:	goto scan33;
			}
scan40:
			++ps->cursor;
#line 108 "sexpr_parse.c.re"
			{ push_control(ps, ';'); continue; }
#line 438 "sexpr_parse.c"
scan42:
			++ps->cursor;
#line 107 "sexpr_parse.c.re"
			{ push_control(ps, '@'); continue; }
#line 443 "sexpr_parse.c"
scan44:
			curr = (unsigned char)*++ps->cursor;
			switch (curr) {
			case '.':	goto scan49;
			default:	goto scan35;
			}
scan45:
			curr = (unsigned char)*++ps->cursor;
			switch (curr) {
			case '0':
			case '1':
			case '2':
			case '3':
			case '4':
			case '5':
			case '6':
			case '7':
			case '8':
			case '9':	goto scan45;
			default:	goto scan47;
			}
scan47:
#line 101 "sexpr_parse.c.re"
			{ push(ps, sp_number(ps, t, ps->cursor, 8)); continue; }
#line 468 "sexpr_parse.c"
scan48:
			yyaccept = 2;
			curr = (unsigned char)*(YYMARKER = ++ps->cursor);
			switch (curr) {
			case 0x00:	goto scan37;
			case '"':	goto scan36;
			case '\\':	goto scan38;
			default:	goto scan33;
			}
scan49:
			++ps->cursor;
#line 104 "sexpr_parse.c.re"
			{ push(ps, sp_symbol(ps, t, ps->cursor)); continue; }
#line 482 "sexpr_parse.c"
		}
#line 112 "sexpr_parse.c.re"

        }
        return res;
}


#ifdef TEST_CASE
/* plain testing that reads s-exprs into a static buffer then prints them out
 * does no memory management at all and just mallocs everything.
 * this representation is only a toy example. */

#include <err.h>
#include <stdio.h>

enum TAG {
        TAG_NONE,
        TAG_CONS,
        TAG_NUMBER,
        TAG_STRING,
        TAG_SYMBOL,
        TAG_UNARY,
        TAG_VECTOR
};

struct value {
        uintptr_t tag;
        uintptr_t rest[];
};

struct value *alloc_value(int n)
{
        return calloc(1, sizeof(struct value) + sizeof(uintptr_t) * n);
}

int estimate_size(uintptr_t n)
{
        int sz = 0;
        struct value *v = (struct value *)n;
        switch (v->tag) {
        case TAG_STRING:
        case TAG_SYMBOL:
                return strlen((char *)v->rest);
        case TAG_NUMBER:
                return snprintf(NULL, 0, "%li", (long)v->rest[0]);
        case TAG_UNARY:
                return estimate_size(v->rest[1]) + 1;
        case TAG_CONS:
                sz += 2;
        case TAG_VECTOR:
                if (!v->rest[0])
                        return 2;
                for (int i = 0; i < v->rest[0]; i++) {
                        sz++;
                        sz += estimate_size(v->rest[i + 1]);
                }
                sz++;
                return sz;
        }
        return 0;
}

void dump_value(uintptr_t u, int n, bool bol)
{
        int sz;
        struct value *v = (struct value *)u;
        switch (v->tag) {
        case TAG_NONE:
                printf("(none)");
                break;
        case TAG_STRING:
        case TAG_SYMBOL:
                printf("%s", (char *)v->rest);
                break;
        case TAG_NUMBER:
                printf("%li", (long)v->rest[0]);
                break;
        case TAG_UNARY:
                printf("%c", (char)v->rest[0]);
                dump_value(v->rest[1], n, false);
                break;
        case TAG_VECTOR:
                if (!v->rest[0]) {
                        printf("()");
                        break;
                }
                sz = estimate_size(u);
                // bool byline =  (n + sz > 40) && v->rest[0] > 2;
                bool byline = sz > 10 &&  v->rest[0] > 2;
                for (int i = 0; i < v->rest[0]; i++) {
                        bool isbol = false;
                        char beg = i ? ' ' : '(';
                        sz = estimate_size(v->rest[i + 1]);
                        if (byline && sz > 7  && !bol) {
                                // && ((struct value *)v->rest[i + 1])->tag == TAG_VECTOR) {
                                printf("\n");
                                for (int j = 0; j < n ; j++)
                                        printf(" ");
                                putc(beg, stdout);
                                isbol = true;
                        } else
                                putc(beg, stdout);
                        dump_value(v->rest[i + 1], n + 1, isbol);
                }
                printf(")");
                break;
        case TAG_CONS:
                printf("(");
                for (int i = 0; i < v->rest[0]; i++) {
                        if (i)
                                printf(" ");
                        dump_value(v->rest[i + 2], n, false);
                }
                printf(" . ");
                dump_value(v->rest[1], n, false);
                printf(")");
        }
}

size_t
my_strlcpy(char * dst, const char * src, size_t maxlen) {
    const size_t srclen = strlen(src);
    if (srclen + 1 < maxlen) {
        memcpy(dst, src, srclen + 1);
    } else if (maxlen != 0) {
        memcpy(dst, src, maxlen - 1);
        dst[maxlen-1] = '\0';
    }
    return srclen;
}

uintptr_t sp_string(struct parse_state *nonce, char *b, char *e)
{
        struct value *s = alloc_value(((e - b + 1) + 7) / sizeof(uintptr_t));
        s->tag = TAG_STRING;
        my_strlcpy((char *)s->rest, b, e - b + 1);
        return (uintptr_t)s;
}
uintptr_t sp_symbol(struct parse_state *nonce, char *b, char *e)
{
        struct value *s = alloc_value(((e - b + 1) + 7) / sizeof(uintptr_t));
        s->tag = TAG_SYMBOL;
        my_strlcpy((char *)s->rest, b, e - b + 1);
        return (uintptr_t)s;
}

uintptr_t sp_number(struct parse_state *nonce, char *b, char *e, int radix)
{
        struct value *s = alloc_value(1);
        s->tag = TAG_NUMBER;
        intptr_t num = 0;
        bool negate = false;
        if (*b == '-') {
                negate = true;
                b++;
        }
        --b;
        while (++b < e)
                num = num * radix + (*b - '0');
        s->rest[0] = (uintptr_t)(negate ? -num : num);
        return (uintptr_t)s;
}


uintptr_t sp_unary(struct parse_state *nonce, char unop, uintptr_t v)
{
        struct value *s = alloc_value(2);
        s->tag = TAG_UNARY;
        s->rest[0] = unop;
        s->rest[1] = v;
        return (uintptr_t)s;
}

uintptr_t sp_list(struct parse_state *nonce, char delim, uintptr_t *start, int len)
{
        struct value *s = alloc_value(len + 1);
        s->tag = TAG_VECTOR;
        s->rest[0] = len;
        memcpy(&s->rest[1], start, len * sizeof(uintptr_t));
        return (uintptr_t)s;
}
/* explicit cons */
uintptr_t sp_cons(struct parse_state *nonce, char delim, uintptr_t *start, int len, uintptr_t cdr)
{
        struct value *s = alloc_value(len + 2);
        s->tag = TAG_CONS;
        s->rest[0] = len;
        s->rest[1] = cdr;
        memcpy(&s->rest[2], start, len * sizeof(uintptr_t));
        return (uintptr_t)s;
}

int
main(int argc, char *argv[])
{
        char buffer[8192] = {};
        fread(buffer, sizeof(buffer) - 1, 1, stdin);
        struct parse_state ps = PARSE_STATE_INITIALIZER;
        scan(&ps, buffer);
        for (int i = 0; i < ps.sptr; i++) {
                dump_value(ps.stack[i], 0,  false);
                printf("\n\n");
        }
        return 0;
}


#endif
